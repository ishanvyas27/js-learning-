let score= false
console.log(typeof score);

let scoreInNum = Number(score);
console.log(scoreInNum);

// " 233" number ins strig can be converted but "23A" will result NaN
// if boolean is converted in Number true => 1 and false =>0
// ""  (empty string)=> false and " ishan" (non empty string)=> true
